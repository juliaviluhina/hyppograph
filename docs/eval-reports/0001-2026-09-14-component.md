# Eval Run 0001 — component — 2026-09-14

## Methodology
- Layer: component (free, offline, `node:test`)
- Judge: none
- Substrate: n/a (no model calls in this layer)

## Under test
- Workflow commit: 280be5b
- Model IDs: n/a (component layer makes no model calls); judge: n/a
- Fixture: none (pure-function unit cases inline in each `*.test.mjs`)
- Run: 2026-09-14 / manual (`node --test evals/component/`, US5 report-writing not yet wired)

## Results
| case | expected | actual | verdict |
|------|----------|--------|---------|
| 37 `node:test` cases across `keys`, `folding`, `criteria`, `sources`, `drift` | all pass | all pass | pass (37/37) |

Independent Test scenarios, run by hand and reverted:
- Reverted `titleKey`'s inner `.toLowerCase()` in `intake-core.mjs` (without re-syncing the inlined
  copy) → suite failed 1/37, naming `drift guard: inlined:intake-core matches …` (the module and the
  workflow's inlined copy diverged, as expected — `slug()` itself re-lowercases, so this specific
  mutation is not independently observable through `titleKey`'s output, only through drift). Restored
  → green.
- Edited one character (`0x811c9dc5` → `...c6`) inside the workflow's `inlined:intake-core` region
  only (module untouched) → `drift.test.mjs` failed, printing the line-level diff and naming the
  region. Restored → green.

## Cost
- Tokens in / out: 0 / 0
- Dollar cost: $0

## Findings
- No defects found — this is the harness's first run, over freshly-authored component tests, not a
  regression check of existing pipeline behaviour.
- Suite duration ~45ms, confirming SC-001 (<5s) with large margin.
- Report assembled by hand per `contracts/eval-report.md` "Generation" — `evals/lib/report.mjs` and
  the `evals/run.mjs` report-writing step land in US5 (T040–T044).

# Eval Run 0010 — integration — 2026-09-15

## Methodology
- Layer: integration
- Judge: none
- Substrate: mock

## Under test
- Workflow commit: c10f2a3
- Model IDs: n/a (mock substrate)
- Fixture: tests/synthetic/data-dir/ (hash not computed)
- Run: manual (node evals/run.mjs integration)

## Results
| case | expected | actual | verdict |
|------|----------|--------|---------|
| gate | matches tests/synthetic/expected/ | exact match | pass |
| idempotency | 0 new files, 0 new activity, provenance byte-identical | confirmed | pass |

## Cost
- Tokens in / out: 0 / 0
- Dollar cost: $0

## Findings
- Part of T047's full quickstart.md sweep (scenarios 1-11 run live; 12 is genuinely manual and
  requires a real board, not run). Running scenario 4 as originally documented
  (`node evals/run.mjs integration`, default `workflow-tool` substrate, fresh scratch dir) surfaced a
  real bug in `gate.mjs`: its "has a real run happened yet" check tested for
  `outputs/job-records/` existing, but that directory is already present the moment the dataset is
  copied (the dataset's own pre-seeded `raw/` records live under it) — so an unrun scratch copy fell
  through to a doomed byte-for-byte comparison instead of the intended setup-required message. Fixed
  to check for `outputs/last-run-summary.md`, which only a completed pipeline run ever writes. This
  run (with `--substrate mock`) is the corrected, passing result.

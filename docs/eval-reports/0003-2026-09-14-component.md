# Eval Run 0003 — component — 2026-09-14

## Methodology
- Layer: component (free, offline, `node:test`)
- Judge: none
- Substrate: n/a

## Under test
- Workflow commit: 807779a
- Model IDs: n/a; judge: n/a
- Fixture: none (unit cases + static source/hygiene checks)
- Run: 2026-09-14 / manual (`node --test evals/component/`, US5 report-writing not yet wired)

## Results
| case | expected | actual | verdict |
|------|----------|--------|---------|
| 57 `node:test` cases (all Phase 3 cases + `tier-guard`, `no-unattended-metered`, `spend`, `credentials`, `credential-hygiene`) | all pass | all pass | pass (57/57) |

## Cost
- Tokens in / out: 0 / 0
- Dollar cost: $0

## Findings
- Adds Phase 5 (US3) guards to the free layer: the system-under-test never escalates off the fast
  tier (`tier-guard.test.mjs` — every `agent()` call site passes `model: FAST`, pinned to `"haiku"`);
  no unattended automation exists to run a metered layer (`no-unattended-metered.test.mjs` — no
  `.github/workflows/`, no active git hooks, `npm test` resolves only to `component`); `spend.mjs`'s
  confirm-spend gate and ceiling tracker behave correctly in isolation (`spend.test.mjs`);
  `credentials.mjs` names a missing variable rather than defaulting or prompting
  (`credentials.test.mjs`); no tracked file or eval report contains a live-looking API key
  (`credential-hygiene.test.mjs`).
- No defects found.
- Scope note (see `tasks.md` T030): `--substrate metered` end-to-end behaviour (estimate-then-exit-0,
  ceiling-abort-mid-run) cannot be exercised through a live call path yet — no substrate calls
  `spend.mjs`/`credentials.mjs` until the judge client (US4, T033) or the gated metered substrate
  (T049) exist. The primitives themselves are unit-verified; wiring verification is deferred to
  those tasks.
- Suite duration stayed well under 5s (SC-001 margin), confirming the free layer's cost stays $0 as
  it grows (SC-004's "routine re-check cost trends to zero").

# Personal notes — NOT a job posting

This file exists to exercise FR-003 / the collect-stage "file that isn't a job posting" edge case.
The manual-drop ingest must skip it and record it in `RunSummary.itemsSkipped` with a reason —
never write a Raw Record for it.

- Reminder: follow up with the Wayne recruiter next week
- Question for later: is the Globex role still open?
- Random link I want to keep: https://example.com/some-article

#!/usr/bin/env node
// 005 harness runner — STUB (T001). Real skeleton lands in T008.
// Must exit non-zero so unfinished work is loud, never silently green.
console.error("harness not implemented yet (005 T008)");
process.exit(1);

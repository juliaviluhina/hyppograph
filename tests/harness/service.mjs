#!/usr/bin/env node
// Fixture REST service — STUB (T001). Real implementation lands in T004.
// Must exit non-zero so unfinished work is loud, never silently green.
console.error("fixture service not implemented yet (005 T004)");
process.exit(1);

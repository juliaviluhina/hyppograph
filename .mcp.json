{
  "$comment": "HyppoVisor per-project instance for hyppograph (slug 'hyppograph', port 7359). Literal localhost URL — no env vars, no bearer token; see .claude/skills/hyppovisor/SKILL.md. Only HyppoVisor's read/navigation tools are used (Principle IV). Launch: open -na HyppoVisor --args --instance hyppograph --port 7359",
  "mcpServers": {
    "hyppovisor-hyppograph": {
      "type": "http",
      "url": "http://127.0.0.1:7359/mcp"
    }
  }
}
